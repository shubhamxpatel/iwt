<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        input {
            padding: 10px;
        }
        
        input[type=text] {
            width: 40%;
        }
        
        input[type=submit]:hover {
            background-color: teal;
            cursor: pointer;
        }
        
        form {
            gap: 10px;
        }
        
         ::-webkit-scrollbar {
            display: none;
        }
        
        * {
            margin: 0;
            padding: 0;
        }
        
        body {
            padding: 50px;
            height: 100vh;
            width: 100vw;
            overflow: hidden;
            display: grid;
            grid-template-columns: 1fr;
            grid-template-rows: 1fr 7fr 2fr;
        }
        
        .container {
            display: flex;
            width: 100%;
            padding: 50px;
            justify-content: space-around;
        }
        
        div {
            display: block;
            padding: 5px;
            overflow: scroll;
        }
        
        a {
            padding: 10px;
            display: block;
            text-align: center;
        }
        
        h1 {
            background: linear-gradient(to right, violet, indigo, blue, green, yellow, orange, red);
            -webkit-text-fill-color: transparent;
            -webkit-background-clip: text;
            padding: 20px;
            font-size: 35pt;
            /* border: 1px solid red; */
            text-align: center;
            /* border-radius: 20px; */
        }
    </style>
</head>

<body>
    <center>
        <form action="/users" method="get">
            <input type="text" name="link" id="link">
            <input type="submit" value="search">
        </form>
    </center>


    <div>

        <% data.forEach(function(item){ %>
            <% var r = item %>

                <%   var x = r.split("\"")[1] %>
                    <% if(x.match(/url(\?q)?=(.*?)&amp/)){ %>
                        <% x=(x.match(/url(\?q)?=(.*?)&amp/)!==null)?x.match(/url(\?q)?=(.*?)&amp/)[2]:"" %>
                            <a class="a" href=<%=x %>><%=x %></a>
                            <% } %>


                                <% }) %>
    </div>
    <div>
        <h1>
            Web Page Ranking Using SEO Suggester
        </h1>
    </div>

</body>
<script>
</script>

</html>